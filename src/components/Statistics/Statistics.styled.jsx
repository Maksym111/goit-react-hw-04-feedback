import styled from '@emotion/styled';

const Title = styled.h1`
  font-size: 24px;
  font-weight: 500;
  margin-bottom: 10px;
`;

const StatistList = styled.ul`
  display: flex;
  flex-direction: column;
  gap: 5px;
`;

export { Title, StatistList };
